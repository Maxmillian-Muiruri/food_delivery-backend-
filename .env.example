# Database (example for SQLite)
#DATABASE_TYPE=sqlite
#DATABASE_NAME=database.sqlite

# OpenRouteService API key (optional). If set, driver routing will use ORS to refine route durations.
ORS_API_KEY=

# Optional flag to force using ORS even if API key is present: 'true' or 'false'
USE_ORS=true
